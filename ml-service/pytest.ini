[pytest]
# Lets tests do `from config import ...` / `from src.xxx import ...` the same
# way main.py does, without needing to install this project as a package.
# Requires running pytest from the ml-service/ directory (or `pytest ml-service`).
pythonpath = .
testpaths = tests
